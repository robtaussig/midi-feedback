import React from 'react'

const Header = () => {
	return (
		<div className="header">
			<button onClick={this.clickForTime} >Click for time</button>
			<button onClick={this.downloadMidi} >Download</button>
		</div>
	)
}

export default Header
